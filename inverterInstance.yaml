
# TODO flesh out the description for each field

allOf:
    - $ref: "inverter.yaml"
    - type: object
      properties:
        firmwareVersion:
            type: string
        lastFirmwareUpdate:
            type: string
        inverterSettings:
            $ref: "inverterSettings.yaml"
        inverterMeasurements:
            # TODO This is probably out of place
            $ref: "inverterMeasurements.yaml"
